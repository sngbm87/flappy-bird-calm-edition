diff --git a/game.js b/game.js
index 0000000..1111111 100644
--- a/game.js
+++ b/game.js
@@ -2350,7 +2350,7 @@ const ACHIEVEMENTS = [
   { id: "FirstFlight", name: "First Flight", check: () => state.score >= 1, progress: () => `${Math.min(state.score, 1)}/1` },
   { id: "ZenMaster", name: "Zen Master", check: () => state.score >= 15, progress: () => `${Math.min(state.score, 15)}/15` },
   { id: "ShieldSavior", name: "Shield Savior", check: () => state.shieldsSavedCount >= 1, progress: () => `${Math.min(state.shieldsSavedCount, 1)}/1` },
-  { id: "ThemeExplorer", name: "Theme Explorer", check: () => state.playedThemes.size >= 4, progress: () => `${state.playedThemes.size}/4` },
+  { id: "ThemeExplorer", name: "Theme Explorer", check: () => state.playedThemes.size >= 5, progress: () => `${state.playedThemes.size}/5` },
   { id: "CalmMarathon", name: "Calm Marathon", check: () => state.zenTimeSec >= 300, progress: () => `${Math.min(Math.floor(state.zenTimeSec / 60), 5)}/5 min` },
   // ... rest unchanged
 ];

diff --git a/game.js b/game.js
index 1111111..2222222 100644
--- a/game.js
+++ b/game.js
@@ -2375,8 +2375,13 @@ function checkAchievements() {
     if (isUnlocked && !wasUnlocked) {
       state.unlockedAchievements.add(ach.id);
       el.classList.add("unlocked");
       sfxAchievement();
       hapticPulse(60);
-      spawnParticles(bird.x, bird.y - 30, 18, "#10b981", 4);
-      announce(`Achievement unlocked: ${ach.name}.`);
+      const isThemeExplorer = ach.id === "ThemeExplorer";
+      spawnParticles(bird.x, bird.y - 30, isThemeExplorer ? 26 : 18, isThemeExplorer ? "#f4d35e" : "#10b981", isThemeExplorer ? 7 : 4);
+      const unlockText = isThemeExplorer
+        ? `Achievement unlocked: ${ach.name}. The full palette is yours.`
+        : `Achievement unlocked: ${ach.name}.`;
+      announce(unlockText);
       showToast(`🎉 ${ach.name}`, "achievement");
       syncStatsAndAchievements(true);
     } else if (isUnlocked) {

diff --git a/game.js b/game.js
index 2222222..3333333 100644
--- a/game.js
+++ b/game.js
@@ -2914,6 +2919,22 @@ function setupInput() {
         if (state.phase === "play" && !state.paused) startMusic();
         playTone(523, 0.12, "sine", 0.08);
         setTimeout(() => playTone(659, 0.15, "sine", 0.06), 80);
+
+        const themeNames = { sunset: "Sunset", midnight: "Midnight", rain: "Cozy Rain", aurora: "Aurora", meadow: "Meadow" };
+        const name = themeNames[state.theme] || state.theme;
+        announce(`Theme set to ${name}.`);
+        if (state.theme === "meadow") {
+          announce("Golden hour. Breathe it in.");
+          if (!state._seenMeadowThisSession) {
+            state._seenMeadowThisSession = true;
+            spawnParticles(CONFIG.CANVAS_W * 0.5, CONFIG.CANVAS_H * 0.35, 14, "#f4d35e", 5);
+          }
+          if (state.playedThemes.size >= 5) {
+            announce("The full palette is yours. This is the calmest one.");
+          }
+        }
       }
     });
   });

diff --git a/game.js b/game.js
index 3333333..4444444 100644
--- a/game.js
+++ b/game.js
@@ -2193,7 +2193,7 @@ function applyTheme(theme) {
   const metaTheme = document.querySelector('meta[name="theme-color"]');
   if (metaTheme) {
-    const palette = { sunset: "#180927", midnight: "#020206", rain: "#0a0f1d", aurora: "#030f0c" };
+    const palette = { sunset: "#180927", midnight: "#020206", rain: "#0a0f1d", aurora: "#030f0c", meadow: "#1a140f" };
     metaTheme.setAttribute("content", palette[nextTheme] || "#081120");
   }

diff --git a/index.html b/index.html
index 4444444..5555555 100644
--- a/index.html
+++ b/index.html
@@ -271,7 +271,7 @@
         <div class="achievement-item" id="achThemeExplorer">
           <span class="achievement-badge">🎨</span>
           <div class="achievement-details">
             <h4 class="achievement-title">Theme Explorer</h4>
-            <p class="achievement-desc">Play under all four visual themes</p>
+            <p class="achievement-desc">Play under all five visual themes</p>
             <p class="achievement-progress"></p>
           </div>
           <span class="achievement-check" aria-hidden="true">✔</span>
         </div>

diff --git a/style.css b/style.css
index 6666666..7777777 100644
--- a/style.css
+++ b/style.css
@@ -767,7 +767,7 @@
   .theme-cluster {
     display: grid;
-    grid-template-columns: repeat(auto-fit, minmax(88px, 1fr));
+    grid-template-columns: repeat(auto-fit, minmax(72px, 1fr));
     gap: 0.5rem;
   }
